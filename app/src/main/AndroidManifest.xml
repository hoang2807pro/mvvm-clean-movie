<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

<!--Permissions-->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

<!--Application-->
<application
    android:allowBackup="true"
    android:name=".AndroidApplication"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/AppTheme">

  <!--Activities-->
    <activity
        android:name="com.fernandocejas.sample.core.navigation.RouteActivity"
        android:noHistory="true"
        android:theme="@style/AppTheme.Translucent"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>

    <activity android:name=".features.home.ui.HomeActivity" android:label="home" />
    <activity android:name=".features.start.ui.StartActivity" android:label="start" />
    <activity android:name=".features.language.ui.LanguageActivity" />

</application>

</manifest>

